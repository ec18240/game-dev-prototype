using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptEventTrigger : MonoBehaviour
{
    public MonoBehaviour [] scriptList;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void runScript()
    {
        for(int index = 0; index<scriptList.Length; index++)
        {
            
        }
    }
}
